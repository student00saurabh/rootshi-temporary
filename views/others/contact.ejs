<% layout("/layouts/boilerplate") -%>


<div class="min-h-screen bg-gray-50 dark:bg-gray-900 transition-colors duration-300">
    <div class="py-16 sm:py-20">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <!-- Header -->
            <div class="text-center mb-16">
                <h1 class="text-4xl md:text-5xl font-bold text-gray-900 dark:text-white mb-6">
                    Contact <span class="text-cyan-600 dark:text-cyan-300">Us</span>
                </h1>
                <p class="text-xl text-gray-600 dark:text-gray-400 max-w-3xl mx-auto">
                    Get in touch with us. We're here to help you with any questions about our courses, 
                    certifications, or anything else.
                </p>
            </div>

            <div class="grid lg:grid-cols-2 gap-12">
                <!-- Contact Form -->
                <div class="bg-white dark:bg-gray-800 rounded-2xl p-8 border border-gray-200 dark:border-gray-700 shadow-lg">
                    <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-6">
                        Send us a message
                    </h2>
                    
                    <form 
  action="/contact" 
  method="POST" 
  class="space-y-6"
>

  <!-- CSRF Token -->
  

  <!-- Name -->
  <div>
    <label for="name" class="block text-sm font-medium mb-2">
      Your Name
    </label>
    <input
      type="text"
      id="name"
      name="name"
      required
      class="w-full px-4 py-3 border rounded-xl
    border-gray-300 dark:border-gray-600
    bg-white dark:bg-gray-800
    text-gray-900 dark:text-gray-100
    placeholder-gray-400 dark:placeholder-gray-500
    focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500
    hover:border-blue-400
    transition-colors duration-200
    valid:border-green-500
    dark:valid:bg-gray-800 dark:valid:text-gray-100 "
      placeholder="John Doe"
    />
  </div>

  <!-- Email -->
  <div>
    <label for="email" class="block text-sm font-medium mb-2">
      Email Address
    </label>
    <input
      type="email"
      id="email"
      name="email"
      required
      class=" w-full px-4 py-3 border rounded-xl
      border-gray-300 dark:border-gray-600
      bg-gray-50 dark:bg-gray-800
      text-gray-900 dark:text-gray-100
      placeholder-gray-400 dark:placeholder-gray-500
      focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500
      hover:border-blue-400
      transition-colors duration-200"
      placeholder="you@example.com"
    />
  </div>

  <!-- Subject -->
  <div>
    <label for="subject" class="block text-sm font-medium mb-2">
      Subject
    </label>
    <select
      id="subject"
      name="subject"
      required
      class="w-full px-4 py-3 border rounded-xl
    border-gray-300 dark:border-gray-600
    bg-white dark:bg-gray-800
    text-gray-900 dark:text-gray-100
    placeholder-gray-400 dark:placeholder-gray-500
    focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500
    hover:border-blue-400
    transition-colors duration-200
    valid:border-green-500
    dark:valid:bg-gray-800 dark:valid:text-gray-100"
    >
      <option value="" disabled selected>Select a subject</option>
      <option value="Course Inquiry">Course Inquiry</option>
      <option value="Certification Questions">Certification Questions</option>
      <option value="Technical Support">Technical Support</option>
      <option value="Billing and Payments">Billing and Payments</option>
      <option value="Partnership Opportunities">Partnership Opportunities</option>
      <option value="other">Other</option>
    </select>
  </div>




  <!-- Message -->
  <div>
    <label for="message" class="block text-sm font-medium mb-2">
      Message
    </label>
    <textarea
      id="message"
      name="message"
      rows="5"
      required
      class="w-full px-4 py-3 border rounded-xl
    border-gray-300 dark:border-gray-600
    bg-white dark:bg-gray-800
    text-gray-900 dark:text-gray-100
    placeholder-gray-400 dark:placeholder-gray-500
    focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500
    hover:border-blue-400
    transition-colors duration-200
    valid:border-green-500
    dark:valid:bg-gray-800 dark:valid:text-gray-100"
      placeholder="How can we help you?"
    ></textarea>
  </div>

  <!-- Location Checkbox -->
  <div class=" items-center space-x-3 p-4 border rounded-xl">
    <input
      type="checkbox"
      id="shareLocation"
      name="shareLocation"
      class="h-5 w-5 text-blue-600 rounded"
    />
    <label for="shareLocation" class="text-sm font-medium cursor-pointer">
      Share my location (optional)
    </label>
    <div id="locationStatus" class="text-xs text-gray-500 ml-auto">
      Not shared
    </div>
  </div>

  <!-- Location fields (hidden by default) -->
  <div id="locationFields" class="hidden space-y-4 p-2 border rounded-xl bg-gray-50">
    <div class="flex space-x-4">
      <div class="flex-1">
        <label class="block text-xs font-medium mb-1 text-gray-600">
          Latitude
        </label>
        <input
          type="text"
          name="latitude"
          readonly
          class="w-full px-3 py-2 border rounded-lg bg-gray-100 text-gray-700"
          placeholder="Location not detected"
        />
      </div>
      <div class="flex-1">
        <label class="block text-xs font-medium mb-1 text-gray-600">
          Longitude
        </label>
        <input
          type="text"
          name="longitude"
          readonly
          class="w-full px-3 py-2 border rounded-lg bg-gray-100 text-gray-700"
          placeholder="Location not detected"
        />
      </div>
    </div>
    <div id="locationError" class="text-xs text-red-600 hidden"></div>
  </div>

  <!-- Location (optional, manual for now) -->
  <!-- <input type="hidden" name="latitude">
  <input type="hidden" name="longitude"> -->

  <!-- Submit -->
  <button
    type="submit"
    class="w-full py-3 bg-blue-600 text-white rounded-xl"
  >
    Send Message
  </button>

</form>


                    <!-- Success Message -->
                    <div id="successMessage" class="hidden mt-4 p-4 bg-green-50 dark:bg-green-900/30 border border-green-200 dark:border-green-800 rounded-xl">
                        <div class="flex items-center">
                            <svg class="w-5 h-5 text-green-500 dark:text-green-400 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                            </svg>
                            <span class="text-green-800 dark:text-green-300">Message sent successfully! We'll get back to you soon.</span>
                        </div>
                    </div>
                </div>

                <!-- Contact Information -->
                <div class="space-y-8">
                    <!-- Email -->
                    <div class="bg-white dark:bg-gray-800 rounded-2xl p-6 border border-gray-200 dark:border-gray-700">
                        <div class="flex items-start">
                            <div class="flex-shrink-0">
                                <div class="w-12 h-12 bg-gradient-to-br from-cyan-500 to-blue-500 rounded-xl flex items-center justify-center">
                                    <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89-5.26a2 2 0 012.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
                                    </svg>
                                </div>
                            </div>
                            <div class="ml-4">
                                <h3 class="text-lg font-bold text-gray-900 dark:text-white">Email Us</h3>
                                <p class="text-gray-600 dark:text-gray-400 mt-1">
                                    For general inquiries and support
                                </p>
                                <a href="mailto:info@rootshield.in" class="text-cyan-600 dark:text-cyan-300 hover:text-cyan-500 dark:hover:text-cyan-400 transition-colors block mt-2">
                                    info@rootshield.in
                                </a>
                            </div>
                        </div>
                    </div>

                    <!-- Business Hours -->
                    <div class="bg-white dark:bg-gray-800 rounded-2xl p-6 border border-gray-200 dark:border-gray-700">
                        <div class="flex items-start">
                            <div class="flex-shrink-0">
                                <div class="w-12 h-12 bg-gradient-to-br from-blue-500 to-purple-500 rounded-xl flex items-center justify-center">
                                    <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
                                    </svg>
                                </div>
                            </div>
                            <div class="ml-4">
                                <h3 class="text-lg font-bold text-gray-900 dark:text-white">Business Hours</h3>
                                <div class="text-gray-600 dark:text-gray-400 mt-1 space-y-1">
                                    <p class="flex justify-between">
                                        <span>Monday - Friday:</span>
                                        <span class="font-medium">9:00 AM - 6:00 PM</span>
                                    </p>
                                    <p class="flex justify-between">
                                        <span>Saturday:</span>
                                        <span class="font-medium">10:00 AM - 4:00 PM</span>
                                    </p>
                                    <p class="flex justify-between">
                                        <span>Sunday:</span>
                                        <span class="font-medium">Closed</span>
                                    </p>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Location -->
                    <div class="bg-white dark:bg-gray-800 rounded-2xl p-6 border border-gray-200 dark:border-gray-700">
                        <div class="flex items-start">
                            <div class="flex-shrink-0">
                                <div class="w-12 h-12 bg-gradient-to-br from-purple-500 to-pink-500 rounded-xl flex items-center justify-center">
                                    <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
                                    </svg>
                                </div>
                            </div>
                            <div class="ml-4">
                                <h3 class="text-lg font-bold text-gray-900 dark:text-white">Our Location</h3>
                                <p class="text-gray-600 dark:text-gray-400 mt-1">
                                    RootShield - Udyam Registered<br>
                                    Delhi, India
                                </p>
                                <p class="text-sm text-gray-500 dark:text-gray-500 mt-2">
                                    *We are an online education platform. All our courses are delivered digitally.
                                </p>
                            </div>
                        </div>
                    </div>

                    <!-- FAQ -->
                    <!-- <div class="bg-gradient-to-r from-blue-600 to-cyan-500 rounded-2xl p-6 text-white">
                        <h3 class="text-lg font-bold mb-4">Common Questions</h3>
                        <div class="space-y-3">
                            <div>
                                <p class="font-medium">Are the certifications recognized?</p>
                                <p class="text-blue-100 text-sm mt-1">
                                    Yes, we provide Udyam-registered certifications that validate your skills.
                                </p>
                            </div>
                            <div>
                                <p class="font-medium">How long do I have access to courses?</p>
                                <p class="text-blue-100 text-sm mt-1">
                                    Most courses offer lifetime access once purchased.
                                </p>
                            </div>
                            <div>
                                <p class="font-medium">Do you offer refunds?</p>
                                <p class="text-blue-100 text-sm mt-1">
                                    Yes, we offer a 7-day refund policy if no course content has been accessed.
                                </p>
                            </div>
                        </div>
                        <a href="/faq" class="inline-flex items-center text-white font-medium mt-4 hover:text-blue-100 transition-colors">
                            View all FAQs
                            <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                            </svg>
                        </a>
                    </div> -->
                </div>
            </div>
        </div>
    </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
  const shareLocationCheckbox = document.getElementById('shareLocation');
  const locationFields = document.getElementById('locationFields');
  const latitudeInput = document.querySelector('input[name="latitude"]');
  const longitudeInput = document.querySelector('input[name="longitude"]');
  const locationStatus = document.getElementById('locationStatus');
  const locationError = document.getElementById('locationError');

  // Toggle location fields when checkbox is clicked
  shareLocationCheckbox.addEventListener('change', function() {
    if (this.checked) {
      // Show loading state
      locationStatus.textContent = 'Detecting location...';
      locationStatus.className = 'text-xs text-yellow-600 ml-auto';
      //locationFields.classList.remove('hidden');
      
      // Get user's location
      getLocation();
    } else {
      // Hide location fields and clear values
      locationFields.classList.add('hidden');
      latitudeInput.value = '';
      longitudeInput.value = '';
      locationStatus.textContent = 'Not shared';
      locationStatus.className = 'text-xs text-gray-500 ml-auto';
      locationError.classList.add('hidden');
    }
  });

  function getLocation() {
    if (!navigator.geolocation) {
      showLocationError('Geolocation is not supported by your browser');
      shareLocationCheckbox.checked = false;
      return;
    }

    // Options for geolocation
    const options = {
      enableHighAccuracy: true, // Try to get precise location
      timeout: 10000, // 10 seconds timeout
      maximumAge: 0 // Don't use cached location
    };

    navigator.geolocation.getCurrentPosition(
      // Success callback
      function(position) {
        const lat = position.coords.latitude;
        const lng = position.coords.longitude;
        
        // Update inputs with location data
        latitudeInput.value = lat.toFixed(6);
        longitudeInput.value = lng.toFixed(6);
        
        // Update status
        locationStatus.textContent = 'Location shared ✓';
        locationStatus.className = 'text-xs text-green-600 ml-auto';
        locationError.classList.add('hidden');
        
        // Optional: Show a small success message
        showTemporaryMessage('Location detected successfully!', 'success');
      },
      // Error callback
      function(error) {
        let errorMessage = 'Unable to retrieve your location. ';
        
        switch(error.code) {
          case error.PERMISSION_DENIED:
            errorMessage += 'Please allow location access in your browser settings.';
            break;
          case error.POSITION_UNAVAILABLE:
            errorMessage += 'Location information is unavailable.';
            break;
          case error.TIMEOUT:
            errorMessage += 'Location request timed out.';
            break;
          default:
            errorMessage += 'An unknown error occurred.';
        }
        
        showLocationError(errorMessage);
        shareLocationCheckbox.checked = false;
        locationFields.classList.add('hidden');
      },
      options
    );
  }

  function showLocationError(message) {
    locationError.textContent = message;
    locationError.classList.remove('hidden');
    locationStatus.textContent = 'Failed to detect';
    locationStatus.className = 'text-xs text-red-600 ml-auto';
  }

  function showTemporaryMessage(message, type) {
    // Create a temporary message element
    const messageDiv = document.createElement('div');
    messageDiv.textContent = message;
    messageDiv.className = `fixed bottom-4 right-4 px-4 py-2 rounded-lg text-sm ${
      type === 'success' ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800'
    }`;
    
    document.body.appendChild(messageDiv);
    
    // Remove after 3 seconds
    setTimeout(() => {
      messageDiv.remove();
    }, 3000);
  }

  // Add privacy notice
  const privacyNotice = document.createElement('div');
  privacyNotice.className = 'mt-2 text-xs text-gray-500';
  privacyNotice.innerHTML = `
    <p>Your location data will only be used to provide better service and will not be shared with third parties.</p>
  `;
  shareLocationCheckbox.parentNode.appendChild(privacyNotice);
});
</script>